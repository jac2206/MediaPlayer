
@{
    ViewBag.Title = "Layout Media Player";
}

<script id="tree-view-template" type="text/x-kendo-template">

        @(Html.Kendo().TreeView()
            .Name("treeview")
            .Events(events => events
                .Select("onSelect")
            )
            .Items(treeview =>
            {
                treeview.Add().Text("Someday");
                treeview.Add().Text("Under Cover of Darkness"); 
                treeview.Add().Text("The Adults Are Talking"); 
            })
            .DataTextField("title")
            .DataSource(dataSource => dataSource
            .Read(read => read
                .Action("ObtenerNombreVideosLista", "LayoutMediaPlayer")
            )
        )
            .ToClientTemplate()
        )
</script>

@*<script id="titulo-video-template" type="text/x-kendo-template">
                @(Html.Label("TituloVideo", "Titulo Video", new { @id = "tituloVideo" })
                )
    </script>*@

<script id="media-player-template" type="text/x-kendo-template">
        @(Html.Kendo().MediaPlayer()
            .AutoPlay(true)
            .Navigatable(true)
            .Media(m => m
                .Title("The Strokes")
            .Source("https://www.youtube.com/watch?v=_l09H-3zzgA"))
            .Name("mediaPlayer")
            .HtmlAttributes(new { style = "height:360px" })
            .AutoPlay(false)
            .ToClientTemplate()
            )
</script>

@*<script id="descripcion-template" type="text/x-kendo-template">
            @(Html.Kendo().TextArea()
                .Name("textarea")
                .Rows(10)
                //.Events(e => e.Change("onChange"))
                //.HtmlAttributes(new { style = "width: 100%; color:#ff0000 ; font-size: 15px;" })
                //.Enable(false)
                //.Readonly(true)
                .ToClientTemplate()
            )
    </script>*@


@*@(Html.Kendo().TileLayout()
        .Name("tilelayout")
        .Columns(3)
        .RowsHeight("500px")
        .ColumnsWidth("500px")
        .Containers(c => {
            c.Add().Header(h => h.Text("Titulo Video")).BodyTemplateId("titulo-video-template").ColSpan(5).RowSpan(1);
            c.Add().Header(h => h.Text("Tree View")).BodyTemplateId("tree-view-template").ColSpan(1).RowSpan(1);
            c.Add().Header(h => h.Text("Media Player")).BodyTemplateId("media-player-template").ColSpan(2).RowSpan(1);
            c.Add().Header(h => h.Text("Descripcion Video")).BodyTemplateId("descripcion-template").ColSpan(3).RowSpan(1);
        })
        .Reorderable(true)
        .Resizable(true)
    //.Events(e=>e.Resize("onTileResize"))
    )*@

<div>
    <h1>
        @Html.Label("TituloVideo", "Titulo Video", new { @id = "tituloVideo", width = "100%", style = "color:#ff0000" })
    </h1>
</div>

<div>

    @(Html.Kendo().TileLayout()
        .Name("tilelayout")
        .Columns(3)
        .RowsHeight("500px")
        .ColumnsWidth("500px")
        .Containers(c => {
            c.Add().Header(h => h.Text("Lista Reproduccion")).BodyTemplateId("tree-view-template").ColSpan(1).RowSpan(1);
            c.Add().Header(h => h.Text("")).BodyTemplateId("media-player-template").ColSpan(2).RowSpan(1);
        })
        .Reorderable(true)
        .Resizable(true)
    //.Events(e=>e.Resize("onTileResize"))
    )
</div>

<div>
    @(Html.Kendo().TextArea()
            .Name("textarea")
            .Rows(10)
            //.Events(e => e.Change("onChange"))
            .HtmlAttributes(new { style = "width: 100%; color:#ff0000 ; font-size: 15px;" })
            //.Enable(false)
            .Readonly(true)
        )
</div>

<script>
    function onSelect(e) {
        var nombreContenido = this.text(e.node)
                $.ajax({
            type: "POST",
            url: '@Url.Action("MostrarContenido", "LayoutMediaPlayer")',
                    data: { "nombreContenido": nombreContenido},
            dataType: "json",
            success: function (result) {
                $("#mediaPlayer").data("kendoMediaPlayer").media({ title: result.titulo, source: result.source });
                $("#tituloVideo").text(result.titulo);
                $("#textarea").data("kendoTextArea").value(result.descripcion);
            },
            error: function (emp) {
               console.log("Not OK")
            }
            });
    }
</script>
